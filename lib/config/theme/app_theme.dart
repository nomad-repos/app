import 'package:flutter/material.dart';

class AppTheme {
  ThemeData getTheme() => ThemeData(
      //Aca adentro van los temas personalizados de la aplicación.
      colorSchemeSeed: const Color.fromRGBO(51, 101, 138, 1),
    );
  }
